# Cycles Subcommands

Planning cycles are read-only. They group reports into half-year periods.

## cycles list

Call `mcp__synka__list_planning_cycles`. Display as table: ID, Name, Start Date, End Date.
